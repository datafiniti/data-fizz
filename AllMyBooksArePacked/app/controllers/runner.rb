# Iterate over each document in data
# for each document, call all parsable methods
# separate each bit of information
# save each bit of information to db

get '/' do
  # file = Parser.process_file('data/book1.html')
  # title = Parser.grab_title(file)
  # author = Parser.grab_author(file)
  # price = Parser.grab_price(file)
  # weight = Parser.grab_weight(file)
  # isbn = Parser.grab_isbn(file)
  # puts "Title: #{title} \n
  # Author: #{author} \n
  # Price: #{price} \n
  # Weight: #{weight} \n
  # ISBN: #{isbn}"
end
