<%= simple_form_for(:book, url: upload_books_path) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :file, as: :file, input_html: { multiple: true } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
