***SETUP***

[Y]-Generate rails api

[Y]-Make models, schemas, and controllers for Database.
  [Y]-Two main objects:
    [Y]-Book
      [Y]-#EDIT# change ISBN to string in case it includes letter
    [Y]-Box

[Y]-Additional Gems I could use?
  [Y]-Nokogiri
  [Y]-open-uri ~ Standard in ruby
  [Y]-rspec for rails

-Setup indexes for both objects (json API)
  -book
  -boxes
  -Return them in json


***Testing***

- Create rspec tests for:
  -Box Model
  -Book Model
  [Y]-Box index (make sure they print to the screen in json)
  [Y]-Book index (make sure they print to the screen in json)
  -Class Helpers


***OO Scraping***

-Classes?
  [Y]-Web scraper
  [Y]-Parse the html (SINGLE RESPONSIBLITY)
    [Y]-You need:
      [Y]-title
      [Y]-author
      [Y]-price
      [Y]-shipping weight
      [Y]-ISBN-10
  -Push the book to the database?
  -make a box?
    -made a Packer class that creates boxes and stuffs them with books
  -optimize box space?

***Database***

  [Y]-create
  [Y]-migrate
  [Y]-seed
    -loop through book class and create 'em
    -books
    -put books in boxes?


