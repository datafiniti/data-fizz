var Book = require('./book');
var Box = require('./box');
var Handler = require('./handler');
var Product = require('./product');

module.exports = {
	Book: Book,
	Box: Box,
	Handler: Handler,
	Product: Product
};
